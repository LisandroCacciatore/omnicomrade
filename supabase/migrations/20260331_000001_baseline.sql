-- Baseline migration scaffold
-- NOTE: replace with real DDL statements before production use.

-- Example:
-- create table if not exists public.example (
--   id uuid primary key,
--   created_at timestamptz default now()
-- );
